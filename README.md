# texture-packer-css-exporter
exporter for texturepacker (specifically tailored to my needs in projects)

##-checked
builds an input + #{name}:hover selector for costum checkbox/radiobuttons

##-selected
builds a #{name}.current selector